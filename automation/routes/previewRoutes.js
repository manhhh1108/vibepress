// Preview proxy đã chuyển sang ai_pipeline.
// File này không còn cần thiết nhưng giữ lại để tránh import error.
const express = require('express');
const router = express.Router();
module.exports = router;
